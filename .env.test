# Integration test environment — auto-discovered from running LMStudio instance
# LMStudio is serving at port 1234 (OpenAI-compatible API default)
LMSTUDIO_URL=http://localhost:1234/v1

# Port the bridge will use during integration tests
PORT=11434

# Models discovered from GET /v1/models
TEST_CHAT_MODEL=qwen3.5-4b-mlx
TEST_EMBED_MODEL=text-embedding-nomic-embed-text-v1.5
